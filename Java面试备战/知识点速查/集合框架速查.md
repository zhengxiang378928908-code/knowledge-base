# Java 集合框架

## 体系结构

```
Collection
├── List（有序，可重复）
│   ├── ArrayList   — 数组实现，查询 O(1)，增删 O(n)
│   ├── LinkedList  — 双向链表，增删 O(1)，查询 O(n)
│   └── Vector      — 线程安全的 ArrayList（已过时）
├── Set（无序，不可重复）
│   ├── HashSet     — HashMap 实现，O(1)
│   ├── LinkedHashSet — 保持插入顺序
│   └── TreeSet     — 红黑树，有序，O(log n)
└── Queue
    ├── LinkedList
    ├── PriorityQueue — 堆实现
    └── ArrayDeque   — 双端队列

Map（键值对）
├── HashMap        — 数组+链表+红黑树（Java 8+）
├── LinkedHashMap  — 保持插入/访问顺序
├── TreeMap        — 红黑树，key 有序
├── Hashtable      — 线程安全（已过时）
└── ConcurrentHashMap — 分段锁/CAS 线程安全
```

## HashMap 深度解析

### 底层结构（Java 8+）

- 数组 + 链表 + 红黑树
- 默认容量 16，负载因子 0.75
- 链表长度 >= 8 且数组长度 >= 64 时转红黑树
- 红黑树节点 <= 6 时退化为链表

### put 流程

1. 计算 key 的 hash → `(h = key.hashCode()) ^ (h >>> 16)`
2. 定位桶 → `(n - 1) & hash`
3. 桶为空 → 直接放入
4. 桶不为空 → 链表/红黑树查找
5. key 存在 → 覆盖 value
6. key 不存在 → 尾插法加入链表
7. 检查是否需要树化 / 扩容

### 扩容机制

- 容量翻倍（`newCap = oldCap << 1`）
- 重新 hash 分配：`hash & oldCap == 0` 留原位，否则移到 `原位 + oldCap`

### 为什么容量必须是 2 的幂？

`(n - 1) & hash` 等价于 `hash % n`，位运算更高效，且分布均匀。

## ConcurrentHashMap

### Java 7

- Segment 分段锁（默认 16 段）
- 每个 Segment 是一个 ReentrantLock + HashEntry 数组

### Java 8+

- 取消 Segment，改用 `Node 数组 + CAS + synchronized`
- 空桶用 CAS 插入
- 非空桶用 synchronized 锁住头节点
- 支持并发扩容

## ArrayList vs LinkedList

| 对比 | ArrayList | LinkedList |
|------|-----------|------------|
| 底层 | 动态数组 | 双向链表 |
| 随机访问 | O(1) | O(n) |
| 头部插入 | O(n) | O(1) |
| 尾部插入 | 均摊 O(1) | O(1) |
| 内存 | 连续，缓存友好 | 分散，额外指针开销 |

> **实际开发中 ArrayList 几乎总是更好**，因为 CPU 缓存局部性优势显著。

## 高频面试题

1. **HashMap 是线程安全的吗？多线程下会出什么问题？**
   - 不安全。Java 7 并发扩容可能死循环（头插法），Java 8 改为尾插法解决了死循环但仍有数据覆盖丢失问题
   - 多线程用 ConcurrentHashMap

2. **HashMap 和 Hashtable 区别？**
   - Hashtable 线程安全（synchronized 锁整个表）、不允许 null key/value、已过时
   - HashMap 非线程安全、允许 null key/value、性能好

3. **ConcurrentHashMap 怎么保证线程安全？**
   - Java 7：Segment 分段锁，每段独立加锁
   - Java 8：CAS + synchronized 锁桶头节点，粒度更细，并发度更高

4. **HashMap key 可以为 null 吗？ConcurrentHashMap 呢？**
   - HashMap 允许一个 null key（放在 table[0]）
   - ConcurrentHashMap 不允许 null key/value（避免二义性：get 返回 null 分不清是 key 不存在还是 value 为 null）

5. **fail-fast 和 fail-safe 的区别？**
   - fail-fast：遍历时修改集合直接抛 ConcurrentModificationException（ArrayList、HashMap 的迭代器）
   - fail-safe：遍历的是副本，不会抛异常（ConcurrentHashMap、CopyOnWriteArrayList）
